#ifndef _CORE_ANIMATION_ANIMATION_H_
#define _CORE_ANIMATION_ANIMATION_H_

#include <variant>

namespace core
{



class AnimationBuilder
{
public:
	AnimationBuilder();
	~AnimationBuilder();
	// AnimationBuilder(tvg::LottieAnimation* animation);
	// float totalFrame();
	// float duration();
	// void frameUpdate(float no);
	// void updateCurrentFrame();

	// void pick(int x, int y);

	// void onUpdateKeyframe(KeyframeVar key);
	// // void onDeleteKeyframe(KeyframeVar key);
	// void onAddShapeLayer();
	// void onRemoveShapeLayer();
	// float mCurrentFrame = 0.0f;

private:
};

}	 // namespace core

#endif